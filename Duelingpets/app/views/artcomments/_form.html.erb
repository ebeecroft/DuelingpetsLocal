<%= form_for([@art, @artcomment]) do |f| %>
  <% if @artcomment.errors.any? %>
    <div id="errorexplan">
      <h2 id="errorheader"><%= pluralize(@artcomment.errors.count, "error") %> prohibited this artcomment from being saved:</h2>

      <ul>
      <% @artcomment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <br>
  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_area :message %>
  </div>
  <div class="field">
    <%= f.label :critique %><br />
    <%= f.check_box :critique %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
  <br>
  <p class="navlinks"><%= link_to 'Back', subfolder_art_path(@art.subfolder, @art) %></p>
<% end %>
