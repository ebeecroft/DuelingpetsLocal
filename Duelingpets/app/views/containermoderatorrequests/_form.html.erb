<%= form_for([@topiccontainer, @containermoderatorrequest]) do |f| %>
  <% if @containermoderatorrequest.errors.any? %>
    <div id="errorexplan">
      <h2 id="errorheader"><%= pluralize(@containermoderatorrequest.errors.count, "error") %> prohibited this containermoderatorrequest from being saved:</h2>

      <ul>
      <% @containermoderatorrequest.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <br>
  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_area :message %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
  <br>
  <p class="navlinks"><%= link_to 'Back', user_forum_path(@topiccontainer.forum.user, @topiccontainer.forum) %></p>
<% end %>
