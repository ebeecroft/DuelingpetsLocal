<%= form_for([@forum, @foruminvite]) do |f| %>
  <% if @foruminvite.errors.any? %>
    <div id="errorexplan">
      <h2 id="errorheader"><%= pluralize(@foruminvite.errors.count, "error") %> prohibited this foruminvite from being saved:</h2>

      <ul>
      <% @foruminvite.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <br>
  <div class="field">
    <%= f.label :user_id, "Friends" %><br />
    <%= f.collection_select(:user_id, @user, :to_s, :to_s) %><!--vname-->
  </div>
  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_area :message %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
  <br>
  <p class="navlinks"><%= link_to 'Back', user_forum_path(@forum.user, @forum) %></p>
<% end %>
