<%= form_for([@gallery, @mainfolder]) do |f| %>
  <% if @mainfolder.errors.any? %>
    <div id="errorexplan">
      <h2 id="errorheader"><%= pluralize(@mainfolder.errors.count, "error") %> prohibited this mainfolder from being saved:</h2>

      <ul>
      <% @mainfolder.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <br>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
  <br>
  <p class="navlinks"><%= link_to 'Back', user_gallery_path(@gallery.user, @gallery) %></p>
<% end %>
