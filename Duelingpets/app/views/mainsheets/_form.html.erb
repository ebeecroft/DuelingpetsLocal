<%= form_for([@radiostation, @mainsheet]) do |f| %>
  <% if @mainsheet.errors.any? %>
    <div id="errorexplan">
      <h2 id="errorheader"><%= pluralize(@mainsheet.errors.count, "error") %> prohibited this mainsheet from being saved:</h2>

      <ul>
      <% @mainsheet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <br>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
  <br>
  <p class="navlinks"><%= link_to 'Back', user_radiostation_path(@radiostation.user, @radiostation) %></p>
<% end %>
