<%= form_for([@sound, @soundcomment]) do |f| %>
  <% if @soundcomment.errors.any? %>
    <div id="errorexplan">
      <h2 id="errorheader"><%= pluralize(@soundcomment.errors.count, "error") %> prohibited this soundcomment from being saved:</h2>

      <ul>
      <% @soundcomment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <br>
  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_area :message %>
  </div>
  <div class="field">
    <%= f.label :critique %><br />
    <%= f.check_box :critique %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
  <br>
  <p class="navlinks"><%= link_to 'Back', subsheet_sound_path(@sound.subsheet, @sound) %></p>
<% end %>
