<%= form_for([@forum, @topiccontainer]) do |f| %>
  <% if @topiccontainer.errors.any? %>
    <div id="errorexplan">
      <h2 id="errorheader"><%= pluralize(@topiccontainer.errors.count, "error") %> prohibited this topiccontainer from being saved:</h2>

      <ul>
      <% @topiccontainer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <br>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
  <br>
  <p class="navlinks"><%= link_to 'Back', user_forum_path(@forum.user, @forum) %></p>
<% end %>
