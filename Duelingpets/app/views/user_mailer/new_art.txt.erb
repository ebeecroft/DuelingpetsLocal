The artist <%= @art.user.vname %> created a new artwork called <%= @art.title %>.
===============================================

It seems that the artist you are watching has just created some new content.
Since your are using the new watch system you will be able to receive alerts
about this particular artist before anyone else does. So even if the content is
buried you will be able to still know about the content itself.

The content of this artwork is directly below this text.

<%= @art.description %>

<% if @art.image.to_s != "" %>
   

   <%= image_tag(attachments[@art.image.to_s].url) %>
<% end %>


From the Duelingpets Staff
